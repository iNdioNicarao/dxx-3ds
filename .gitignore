# Build artifacts (never commit binaries / crash dumps)
*.cia
*.3dsx
*.dmp
*.dump

# Local build outputs
build/
*.elf

# Local scratch / logs
*.bundle
final_changes.diff
parsed_dump_*.txt
crash_dump_*.dmp

# OS / editor
.DS_Store
Thumbs.db
