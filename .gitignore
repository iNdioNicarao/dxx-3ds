# --- Build artifacts (generated binaries, never commit) ---
*.cia
*.3dsx
*.elf
*.map

# --- Crash / debug dumps & scratch (not source) ---
*.dmp
*.dump
*.bundle
final_changes.diff
parsed_dump_*.txt
crash_dump_*.dmp
diag/

# --- Game data the USER must supply (copyrighted, NOT redistributable) ---
# Place your own descent.hog / descent.pig (from a legally-owned Descent 1) here:
d1/3ds_data/romfs/descent.hog
d1/3ds_data/romfs/descent.pig
# Also the DXX music pack + jingles are user-supplied (see README).

# --- Tooling binaries (download separately; never commit) ---
bannertool
makerom
makerom.zip
d1xr-sc55-music.zip

# --- Debug photos / recordings (analysis inputs, not source) ---
IMG_*.JPEG
*.JPEG
IMG_*.MOV
*.mov
*.mp4

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*~

# Generated 3DS build artifacts (regenerated by make_cia.sh / Docker build)
d1/d1x-3ds.smdh
d1/d1x-3ds.map
d1/3ds_data/banner.bnr
d1/3ds_data/icon.icn
